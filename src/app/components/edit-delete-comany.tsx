import { useContext } from "react"
import { UserContext } from "../(group)/layout"

export default function edit-delete-comany({job}) {
    const {user}=useContext(UserContext)
    if(user.job.id==jobId){
        
    }
  return (
    <div>
      
    </div>
  )
}
